<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Circle background -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#3a7272" /> <!-- Circle color -->
        </shape>
    </item>
    <!-- Mic icon -->
    <item android:drawable="@drawable/flip_camera"
        android:gravity="center"
        android:start="2dp"
        android:end="2dp"
        android:bottom="2dp"
        android:top="2dp"
        android:width="16dp"
        android:height="16dp" />
</layer-list>
