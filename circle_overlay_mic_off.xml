<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Circle background -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#3a7272" /> <!-- Circle color -->
        </shape>
    </item>
    <!-- Mic icon -->
    <item android:drawable="@drawable/ic_mic_off"
        android:gravity="center"
        android:start="2dp"
        android:end="2dp"
        android:bottom="2dp"
        android:top="2dp"
        android:width="14dp"
        android:height="14dp" />
</layer-list>
